# Project Rules: ระบบธรรมศึกษา

## 1. วิธีเริ่มงาน

ก่อนลงมือทุกครั้งให้ทำตามลำดับนี้:

1. อ่าน `AGENTS.md` และไฟล์ skill ที่ระบุไว้
2. ตรวจสถานะ git และดู diff/การแก้ไขค้างของผู้ใช้ ห้ามลบทิ้งหรือเขียนทับ
3. สำรวจโครงสร้างและพฤติกรรมปัจจุบันของหน้าที่เกี่ยวข้อง
4. แยกให้ชัดว่าอะไร “ดีและใช้งานได้แล้ว” กับอะไร “ต้องแก้/ต้องพัฒนา”
5. ทำแผนแก้เฉพาะจุด พร้อมระบุสิ่งที่ตั้งใจไม่แตะ
6. แก้ให้น้อยที่สุด แล้วทดสอบพฤติกรรมจริง

ถ้าข้อมูลหรือสิทธิ์ไม่พอ ให้หยุดเฉพาะส่วนที่เสี่ยงและแจ้งผู้ใช้ ไม่เดา ไม่สร้างข้อมูลแทน

## 2. พฤติกรรมที่ต้องรักษา

- Supabase เป็นฐานข้อมูลหลักและแหล่งข้อมูลจริง
- หน้าเว็บบันทึกข้อมูลสมัครเข้าฐานกลางตามสิทธิ์ของครู นักเรียน และแอดมิน
- Google Sheets เป็นรายงานปลายทางจากข้อมูลใน Supabase ไม่ใช่ฐานข้อมูลสองทาง
- การซิงก์ต้องไม่ append ซ้ำ ต้องจัดข้อมูลตามรหัสนักเรียน/ชั้น/ห้อง และสะท้อนการแก้ไขล่าสุด
- ฟอร์ม ศ.5/ศ.6 และรูปแบบการพิมพ์เดิมต้องไม่ถูกสร้างทับหรือเปลี่ยนโดยไม่จำเป็น
- การจับคู่ใบประกาศต้องเลือกผลสูงสุดของนักเรียนคนเดิมตามกติกาเดิม
- การเลือก “ไม่สอบ” ต้องผ่านเงื่อนไขใบประกาศเดิมหรือสถานะนักเรียนพิเศษ
- หน้าสาธารณะห้ามเปิดเผยเลขประชาชน วันเกิด หรือข้อมูลส่วนบุคคล
- หน้าแอดมินต้องผ่าน Google account/สิทธิ์ที่กำหนด และครู/นักเรียนต้องเห็นเฉพาะขอบเขตของตน
- เดสก์ท็อป ตาราง และรายงานพิมพ์ที่ผ่านการยืนยันแล้ว ห้ามถูกรื้อเพียงเพื่อแก้มือถือ

## 3. แนวทางการแก้ไข

- ใช้ patch ขนาดเล็กและแยก commit ตามเหตุผลของการแก้
- แก้ CSS เฉพาะ breakpoint มือถือเมื่อปัญหาอยู่ที่มือถือ
- แก้ mapping หรือ query เฉพาะคอลัมน์/ฟังก์ชันที่ผิด ไม่เปลี่ยน schema หรือข้อมูลเดิมทั้งชุดโดยไม่จำเป็น
- การเพิ่มระบบซิงก์ทันทีต้องอยู่ฝั่งเซิร์ฟเวอร์เท่านั้น ห้ามส่ง credential ไปเบราว์เซอร์
- ต้องมี idempotency/queue/lock เมื่อมีการซิงก์พร้อมกัน เพื่อไม่ให้เขียนซ้ำหรือชนกัน
- ห้ามเปลี่ยน Supabase ให้เป็นสองทางกับ Google Sheets โดยไม่มีแผน conflict resolution และ audit log

## 4. การตรวจสอบก่อนส่งงาน

อย่างน้อยต้องตรวจ:

- `git diff --check`
- test ที่เกี่ยวข้องกับหน้าเว็บ, migration, roster และ workflow
- การแสดงผล Chrome desktop และ mobile breakpoint หากแก้ UI
- การบันทึก/แก้ไข/โหลดข้อมูลกลับ หากแก้ data flow
- สถานะ workflow และผลซิงก์ หากแก้ worker หรือ mapping
- ตรวจว่าไม่มี secret หรือ PII หลุดใน diff, log และ public assets

หาก test เดิมล้มเหลว ให้แยกก่อนว่าเป็น regression จากงานนี้หรือเป็น test contract ที่ไม่ทัน migration ปัจจุบัน แล้วแก้เฉพาะสาเหตุที่ยืนยันได้

## 5. เกณฑ์ push/deploy

ก่อน push/deploy ต้องรายงานให้ผู้ใช้ทราบ:

- แก้ไฟล์ใดและแก้เพื่ออะไร
- ส่วนใดที่ตั้งใจไม่แตะ
- test ที่ผ่าน/ไม่ผ่าน
- ผลกระทบต่อ Supabase, Google Sheets และสิทธิ์ข้อมูล

ถ้ายังมีความเสี่ยงต่อข้อมูลส่วนบุคคล ความถูกต้องของการจับคู่ หรือการเขียนทับข้อมูล ให้ยังไม่ deploy
